{
  "name": "spokenframe-worker",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/worker.test.mjs"
  },
  "devDependencies": { "wrangler": "^4.35.0" }
}
